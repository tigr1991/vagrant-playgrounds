---
- hosts: localhost
  connection: local
  become: yes

  roles:
    - tigr1991.php71
    - {
        role: "bennojoy.mysql",
        mysql_db: [{name: project}],
        mysql_users: [{name: web, pass: password, host: "%",priv: "*.*:ALL"}],
        mysql_repl_role: "none"
       }